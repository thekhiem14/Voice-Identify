# CPU is the portable baseline. scripts/install_gpu.ps1 upgrades these wheels
# and installs sherpa-onnx CUDA DLLs when GPU mode is wanted.
--extra-index-url https://download.pytorch.org/whl/cpu

# Core audio and model runtimes
numpy>=1.26,<3
soundfile>=0.12
scipy>=1.11
toml>=0.10
huggingface-hub>=0.30,<1
pyyaml>=6
torch==2.11.0+cpu; sys_platform == "win32"
torchaudio==2.11.0+cpu; sys_platform == "win32"
torch>=2.2; sys_platform != "win32"
torchaudio>=2.2; sys_platform != "win32"
sherpa-onnx==1.13.3

# Desktop UI (the layout targets the 0.85 API)
flet>=0.85,<0.86

# DiariZen / 3D-Speaker runtime dependencies
accelerate>=1.6
speechbrain>=0.5.16
torchinfo>=1.8
scikit-learn>=1.3

# Audio enhancement runtime (required when the UI's enhancement switch is on)
noisereduce>=3.0,<4
nara-wpe>=0.0.11,<1
